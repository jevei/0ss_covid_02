<UserControl x:Class="BillingManagement.UI.Views.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BillingManagement.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Margin="6" Orientation="Horizontal" x:Name="spHeader">
                <TextBlock Text="Liste des factures" />
            </StackPanel>
            <ListView Grid.Row="1" ItemsSource="{Binding Invoices}"
                          SelectedItem="{Binding SelectedInvoice}"
                          x:Name="lvInvoices">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding InvoiceID}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="{Binding ElementName=spHeader,Path=ActualHeight}"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="ID :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.InvoiceID}"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="Client :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.Customer.Info}"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="SubTotal :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.SubTotal}"/>
            </StackPanel>
            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="TVA :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.FedTax}"/>
            </StackPanel>
            <StackPanel Grid.Row="5" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="TVQ :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.ProvTax}"/>
            </StackPanel>
            <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Horizontal" Margin="3">
                <TextBlock Text="Total :" Width="75"/>
                <TextBox MinWidth="125" Text="{Binding SelectedInvoice.Total}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
